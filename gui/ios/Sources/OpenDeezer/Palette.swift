import SwiftUI

/// Deezer brand accent + a couple of app-wide layout constants.
enum Palette {
    /// Verified Deezer brand purple.
    static let accent = Color(red: 0xA2 / 255, green: 0x38 / 255, blue: 0xFF / 255)

    static let miniPlayerHeight: CGFloat = 56
}
