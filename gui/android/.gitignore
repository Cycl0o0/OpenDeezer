# Android / Gradle build artefacts
.gradle/
.kotlin/
build/
app/build/
local.properties
*.iml
.idea/
captures/

# The gomobile-bound engine is produced by build.sh / CI, not committed.
app/libs/*.aar
app/libs/*-sources.jar
