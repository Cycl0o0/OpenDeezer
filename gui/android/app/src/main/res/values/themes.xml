<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose-only host theme: a platform Material parent (no Material Components
         dependency needed). Colors are driven by the Compose theme at runtime. -->
    <style name="Theme.OpenDeezer" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
