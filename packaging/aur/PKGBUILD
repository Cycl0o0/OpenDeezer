# Maintainer: Cycl0o0 <cyclolysis0@gmail.com>
# OpenDeezer terminal client (builds from the tagged source).
pkgname=opendeezer
pkgver=3.1.3
pkgrel=1
pkgdesc="Native terminal client for browsing and streaming a Deezer library"
arch=('x86_64' 'aarch64')
url="https://github.com/Cycl0o0/OpenDeezer"
license=('AGPL-3.0-only')
depends=('alsa-lib')
makedepends=('go' 'git')
source=("$pkgname-$pkgver.tar.gz::$url/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('eb493ad18a6f810463267da3279351ba98dbc84719db469f5985733f327c5688')

build() {
  cd "OpenDeezer-$pkgver"
  export CGO_ENABLED=1
  export GOFLAGS="-trimpath -mod=readonly -modcacherw"
  export GOPATH="$srcdir/gopath"
  go build -trimpath -ldflags "-s -w -X main.version=$pkgver" -o opendeezer ./cmd/opendeezer
}

package() {
  cd "OpenDeezer-$pkgver"
  install -Dm755 opendeezer "$pkgdir/usr/bin/opendeezer"
  install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
}
